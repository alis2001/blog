NODE_ENV=development
PORT=3000

# MongoDB Configuration (REQUIRED: Use authentication in production!)
MONGODB_URI=mongodb://username:password@localhost:27017/pahlavi-iran?authSource=admin
MONGODB_URI_PROD=mongodb://username:password@localhost:27017/pahlavi-iran?authSource=admin

# JWT Configuration (REQUIRED: Change these in production!)
JWT_SECRET=CHANGE-THIS-TO-RANDOM-STRING-MIN-32-CHARACTERS
JWT_EXPIRE=7d

# Session Configuration (REQUIRED: Change this in production!)
SESSION_SECRET=CHANGE-THIS-TO-RANDOM-STRING-MIN-32-CHARACTERS

# Admin Credentials (REQUIRED: Change these immediately!)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeThisPassword123!

# Production Settings (Optional)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Security Note:
# 1. Never commit .env file to version control
# 2. Use strong, unique secrets (min 32 characters, random)
# 3. Change all default passwords immediately
# 4. Enable MongoDB authentication in production
# 5. Use HTTPS in production (set NODE_ENV=production)
